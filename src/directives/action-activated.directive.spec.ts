/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ActionActivatedDirective } from './action-activated.directive';

describe('Directive: ActionActivated', () => {
  it('should create an instance', () => {
    const directive = new ActionActivatedDirective();
    expect(directive).toBeTruthy();
  });
});
